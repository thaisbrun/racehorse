<form method="post" action="{{ path('app_equide_delete', {'idequide': equide.idequide}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ equide.idequide) }}">
    <button class="btn">Delete</button>
</form>
