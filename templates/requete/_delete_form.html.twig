<form method="post" action="{{ path('app_requete_delete', {'idrequete': requete.idrequete}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ requete.idrequete) }}">
    <button class="btn">Delete</button>
</form>
