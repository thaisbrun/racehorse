{% extends 'base.html.twig' %}

{% block title %}Annonce{% endblock %}

{% block body %}
<div class="container">
    <br>
    <div class="columns">
        <div class="column is-half">
            <div class="box has-background-warning-light">
                <img src="{{ asset('imgAnnonce/DSC_9358.jpg') }}" alt="Placeholder image">
            </div>
            </div>
        <div class="column">
            <!-- nom equide -->
            <div class="column">
                <h4>{{ annonce.titre }} {{ annonce.prix }} € </h4>
<br>
            <p>{{ equide.nom }}
            {{ equide.dateNaiss|date("d/m/Y") }}
            {{ equide.taille }} cm
            {{ race.libelle }}
            {{ robe.libelle }}
            {{ departement.libelle }}
            {{ region.libelle }}
            {{ user.login }}
            </p>

            <h3><a href="{{ equide.lienhn }}">Voir infos Haras Nationaux</a></h3>
            <br>
            <p>{{ annonce.description }}</p>
            <br>
        </div>
    </div>
</div>
    <a href="{{ path('app_annonce_index') }}">Retour au menu</a>
</div>
    <!-- include('annonce/_delete_form.html.twig') -->
{% endblock %}
