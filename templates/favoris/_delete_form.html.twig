<form method="post" action="{{ path('app_favoris_delete', {'idutilisateurfav': favori.idutilisateurfav}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ favori.idutilisateurfav) }}">
    <button class="btn">Delete</button>
</form>
